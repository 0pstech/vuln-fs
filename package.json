{
  "name": "vuln-fs",
  "version": "2.4.0",
  "description": "Secure MCP server for filesystem vulnerability testing",
  "type": "module",
  "bin": {
    "vuln-fs": "dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc && shx chmod +x dist/*.js",
    "prepare": "npm run build",
    "watch": "tsc --watch"
  },
  "keywords": ["mcp", "modelcontextprotocol", "filesystem", "security", "tool-server"],
  "author": "opstech",
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "1.0.1",
    "zod": "^3.22.4",
    "zod-to-json-schema": "^3.22.4"
  },
  "devDependencies": {
    "@types/node": "^22",
    "shx": "^0.3.4",
    "typescript": "^5.6.2"
  }
}
